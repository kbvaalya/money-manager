# =========================
# Server (Render)
# =========================
server.port=${PORT:8080}
server.servlet.context-path=/api/v1.0

# =========================
# PostgreSQL (Render Internal)
# =========================
spring.datasource.url=jdbc:postgresql://dpg-d69vekv5r7bs73ff5rm0-a:5432/moneymanager_w7la?ssl=true&sslmode=require
spring.datasource.username=moneymanager_w7la_user
spring.datasource.password=vAdzGdw27BNKoiiePKtxXmdRS3P1OSXL

# Optional (helps on some clouds)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000

# =========================
# JPA / Hibernate
# =========================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# =========================
# Mail (Gmail SMTP)
# IMPORTANT: set env vars on Render, or it will use safe defaults and still start
# =========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${GMAIL_USERNAME:}
spring.mail.password=${GMAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from=${GMAIL_FROM_EMAIL:no-reply@example.com}

# =========================
# JWT
# Better to override via env var in Render
# =========================
jwt.secret=${JWT_SECRET:amir2008-super-secret-key-for-jwt-token-generation-2026}

# =========================
# Frontend URL (for CORS/links)
# =========================
money.manager.frontend.url=${MONEY_MANAGER_FRONTEND_URL:http://localhost:3000}

# =========================
# Logs
# =========================
logging.level.org.springframework.security=DEBUG